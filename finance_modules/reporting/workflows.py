"""
Reporting Module Workflows — Stub.

The reporting module is read-only: it has no state machines.
This file exists to maintain the 6-file module convention.
"""

from finance_kernel.logging_config import get_logger

logger = get_logger("modules.reporting.workflows")

# Reporting has no workflows — it is purely a read-only query module.
